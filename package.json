{
  "name": "agent-fleet-test",
  "version": "1.0.0",
  "description": "测试仓库 - Agent 舰队系统功能验证",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "build": "tsc",
    "prepare": "husky install"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "typescript": "^5.3.0",
    "eslint": "^8.57.0",
    "@types/node": "^20.11.0"
  }
}
